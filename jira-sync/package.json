{
  "name": "jira-dashboard-sync",
  "version": "1.0.0",
  "description": "Jira → work-status-dashboard data.json 자동 동기화",
  "type": "module",
  "main": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "sync-once": "node sync.js --once"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.4.0",
    "node-cron": "^3.0.3"
  }
}
